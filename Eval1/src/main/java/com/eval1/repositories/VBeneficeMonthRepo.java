package com.eval1.repositories;

import org.springframework.data.jpa.repository.JpaRepository;
import com.eval1.models.VBeneficeMonth;

public interface VBeneficeMonthRepo extends JpaRepository<VBeneficeMonth, Long> {
}
