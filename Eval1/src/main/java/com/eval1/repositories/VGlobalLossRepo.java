package com.eval1.repositories;

import org.springframework.data.jpa.repository.JpaRepository;
import com.eval1.models.VGlobalLoss;

public interface VGlobalLossRepo extends JpaRepository<VGlobalLoss, Long> {
}
